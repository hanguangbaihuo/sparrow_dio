name: sparrow_dio
description: sparrow团队完成的对dio的封装，实现dio的async await调用以及回调方式的调用.
version: 0.12.0

homepage: https://github.com/hanguangbaihuo/sparrow_dio

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

  connectivity: ^3.0.6 # 检查手机联网情况
  dio: ^4.0.0 # 网络请求
  pretty_dio_logger: ^1.2.0-beta-1
  sparrow_utils: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
